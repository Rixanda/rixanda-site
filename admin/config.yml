backend:
  name: github
  repo: "Rixanda/rixanda-site"
  branch: main

media_folder: "assets/uploads"
public_folder: "/rixanda-site/assets/uploads"

collections:
  - name: "products"
    label: "Products"
    files:
      - file: "data/products.json"
        label: "All products"
        name: "products"
        fields:
          - {label: "Products", name: "products", widget: "list", fields: [
              {label: "ID (no spaces, used in the link)", name: "id", widget: "string"},
              {label: "Name", name: "name", widget: "string"},
              {label: "Category (type any name — new ones appear on the site automatically)", name: "category", widget: "string"},
              {label: "Price (USD, numbers only)", name: "price", widget: "string"},
              {label: "Description", name: "description", widget: "text"},
              {label: "Cover image", name: "cover_image", widget: "image", required: false},
              {label: "More photos (optional)", name: "gallery", widget: "list", field: {label: "Image", name: "image", widget: "image"}, required: false},
              {label: "Product file (PDF)", name: "file_url", widget: "file", required: false}
            ]}
